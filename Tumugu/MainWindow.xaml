<Window x:Class="Tumugu.MainWindow"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Tumugu"
        mc:Ignorable="d"
        
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.FontWeight="Medium"
        TextElement.FontSize="14"
        FontFamily="pack://application:,,,/MaterialDesignThemes.Wpf;component/Resources/Roboto/#Roboto"
        Title="MainWindow" Height="650" Width="900" BorderThickness="1" WindowStartupLocation="Manual"
        ResizeMode="CanResize" WindowStyle="None" Loaded="Window_Loaded">
    <!-- WindowChrome を使ってカスタムタイトルバーを作っている場合、
         CaptionHeight のデフォルト値（約 30px）が 「ドラッグ領域」として確保されるため、
         その高さ分だけ 上部に空白が強制的に入ります。
         解決策：CaptionHeight="0" を明示的に指定する
    -->

    <WindowChrome.WindowChrome>
        <WindowChrome
            CaptionHeight="0"
            ResizeBorderThickness="5"
            GlassFrameThickness="0"
            UseAeroCaptionButtons="False" />
    </WindowChrome.WindowChrome>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>

        <!-- ヘッダー部分 -->
        <Grid Grid.Row="0" Height="45" Background="#FF4E4E4E" Margin="0, -5, 0, 0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="50"/>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="130"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="125"/>
            </Grid.ColumnDefinitions>

            <!-- アイコン -->
            <materialDesign:PackIcon Kind="ChartBubble" Grid.Column="0" Foreground="White" Height="45" Width="45" HorizontalAlignment="Center" VerticalAlignment="Center"/>

            <!-- タイトル -->
            <Canvas Grid.Column="1">
                <StackPanel Orientation="Horizontal">
                    <Label Content="Tu" FontSize="22" FontFamily="Arial Black" Foreground="White" Margin="0,1,0,0"/>
                    <Label Content="m" FontSize="22" FontFamily="Arial Black" Foreground="SteelBlue" Margin="-7,1,0,0"/>
                    <Label Content="ugu" FontSize="22" FontFamily="Arial Black" Foreground="white" Margin="-7,1,0,0"/>
                </StackPanel>
                <Label Content="- Markdown Editor -" FontSize="9" FontFamily="Arial Black" Foreground="WhiteSmoke" Margin="0,27,0,0"/>
            </Canvas>

            <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Left">
                <Button x:Name="BtnFolder" Style="{DynamicResource MaterialDesignToolButton}" Height="40" Width="65" ToolTip="名前をつけて保存" HorizontalAlignment="Center" VerticalAlignment="Center" Click="BtnFolder_Click">
                    <StackPanel Orientation="Vertical" HorizontalAlignment="Center" Height="40" Width="65" >
                        <materialDesign:PackIcon Kind="FolderOpen" Foreground="White" Height="25" Width="25" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        <Label Content="名前をつけて保存" FontSize="8" Foreground="White" HorizontalAlignment="Center" Margin="0,-5,0,0"/>
                    </StackPanel>
                </Button>
                <Button x:Name="BtnSaveAs" Style="{DynamicResource MaterialDesignToolButton}" Height="40" Width="60" ToolTip="上書き保存" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="3" Click="BtnSaveAs_Click">
                    <StackPanel Orientation="Vertical" HorizontalAlignment="Center" Height="40" Width="60" >
                        <materialDesign:PackIcon Kind="FloppyDisk" Foreground="White" Height="25" Width="25" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        <Label Content="上書き保存" FontSize="8" Foreground="White" HorizontalAlignment="Center" Margin="0,-5,0,0"/>
                    </StackPanel>
                </Button>
            </StackPanel>

            <Label x:Name="lblTitleBlankArea" Grid.Column="3" Background="Transparent" HorizontalAlignment="Stretch" 
                       VerticalAlignment="Stretch" MouseDoubleClick="lblTitleBlankArea_MouseDoubleClick" />

            <!-- ウィンドウのボタン -->
            <StackPanel Grid.Column="4" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="BtnMinimize" Style="{DynamicResource MaterialDesignToolButton}" Height="40" Width="40" Click="BtnMinimize_Click" ToolTip="最小化" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <materialDesign:PackIcon Kind="Minus" Foreground="White" Height="25" Width="25" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Button>
                <Button x:Name="BtnMaximize" Style="{DynamicResource MaterialDesignToolButton}" Height="40" Width="40" ToolTip="最大化" HorizontalAlignment="Center" VerticalAlignment="Center" Click="BtnMaximize_Click">
                    <materialDesign:PackIcon Kind="WindowMaximize" Foreground="White" Height="25" Width="25" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Button>
                <Button x:Name="BtnClose" Style="{DynamicResource MaterialDesignToolButton}" Height="40" Width="40" Click="BtnClose_Click" ToolTip="閉じる" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="3">
                    <materialDesign:PackIcon Kind="Close" Foreground="White" Height="25" Width="25" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Button>
            </StackPanel>
        </Grid>

        <!-- ボディ部分 -->
        <Grid Grid.Row="1" Height="Auto" Background="White">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="TextBoxColumn" Width="1*" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition x:Name="MarkdownBrowserColumn" Width="3*" />
            </Grid.ColumnDefinitions>

            <!-- Markdownの入力TextBox -->
            <Border Grid.Row="0" Grid.Column="0" BorderBrush="#A0A0A0" Background="White" BorderThickness="1" CornerRadius="4" Margin="10,10,1,10">
                <RichTextBox x:Name="RichMarkdownTextBox" Grid.Row="0" Grid.Column="0" Background="White" Margin="5"
                               HorizontalAlignment="Stretch"
        VerticalAlignment="Stretch"
VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" AcceptsReturn="True" AcceptsTab="True"
                            AllowDrop="True" TextChanged="RichTextBox_TextChanged" PreviewMouseWheel="RichMarkdownTextBox_PreviewMouseWheel"
                            FontFamily="Consolas" FontSize="16" Drop="RichMarkdownTextBox_Drop" PreviewDragOver="RichMarkdownTextBox_PreviewDragOver">
                    <RichTextBox.Resources>
                        <Style TargetType="{x:Type Paragraph}">
                            <Setter Property="LineHeight" Value="16" />
                            <Setter Property="LineStackingStrategy" Value="BlockLineHeight" />
                            <Setter Property="Margin" Value="0" />
                        </Style>
                    </RichTextBox.Resources>
                    <FlowDocument>
                        <Paragraph></Paragraph>
                    </FlowDocument>
                </RichTextBox>
            </Border>

            <!-- TextBoxとWebView2の中央に左右の画面サイズ変更のため配置 -->
            <!--<GridSplitter Grid.Row="1" Grid.Column="1" Width="6" HorizontalAlignment="Center">
                <GridSplitter.Style>
                    <Style TargetType="GridSplitter">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Cursor" Value="SizeWE"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#414141"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </GridSplitter.Style>
            </GridSplitter>-->

            <!-- MarkdownのPreview(HTML) -->
            <!-- MarkdownをHTMLで表示するため、NugetからMicrosoft.Web.WebView2をインストール -->
            <Border Grid.Row="1" Grid.Column="2" BorderBrush="#A0A0A0" Background="{x:Null}"
                Panel.ZIndex="1" BorderThickness="1" CornerRadius="4" Margin="0, 10, 10, 10">
                <wv2:WebView2 x:Name="MarkdownBrowser" AllowDrop="False" Cursor="Arrow" Panel.ZIndex="2" Margin="5" DragOver="MarkdownBrowser_DragOver"/>
            </Border>
        </Grid>

        <Grid Grid.Row="2" Height="30" Background="#414141">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!--<Label Grid.Column="0" x:Name="lblVersion" Content="Ver 1.0.0" Foreground="White" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,0,0,0" FontSize="12"/>-->
            <CheckBox x:Name="chkSimpleEdit" Grid.Column="0" Content="軽量モード" Foreground="White" Background="AliceBlue" VerticalAlignment="Center" Margin="0,0,10,0" IsChecked="True" Checked="chkSimpleEdit_Checked" Unchecked="chkSimpleEdit_Unchecked" />

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button x:Name="Btn0Percent" Content="なし" Width="60" Height="25" Background="LightGray" Click="Btn0Percent_Click" />
                <Button x:Name="Btn25Percent" Content="25%" Width="60" Height="25" Background="LightGray" Click="Btn25Percent_Click" Margin="5,0,5,0"></Button>
                <Button x:Name="Btn50Percent" Content="50%" Width="60" Height="25" Background="LightGray" Click="Btn50Percent_Click"></Button>
                <Label Content="対象ファイル：" Foreground="White" VerticalAlignment="Center" Margin="10,0,0,0"/>
                <Label x:Name="lblEditFile" Content="C:¥Temp\..." Foreground="White" VerticalAlignment="Center" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
